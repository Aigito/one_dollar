<div class="jumbotron text-center">
  <h1 class="display-4"><%= "Welcome back, #{current_user.first_name} #{current_user.last_name}!"%></h1>
  <p class="lead">Some Random Quotes (To Be Changed): <%= Faker::Movie.quote %></p>
  <hr class="my-4">
  <p>What do you feel like doing today?</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="goals/new" role="button">Add A New Goal</a>
  <br>
  <br>
  </p>
  <p>Invested amount: 250 / <%= @user.investment%></p>
  <p>Note: Currently this is hardcoded, will need to implement JS to make it dynamic</p>
  <div class="container progress">
    <div class="progress-bar progress-bar-striped bg-success" role="progressbar" style="width: 25%" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100"></div>
  </div>
</div>

<div class="card-group pt-5">
  <% @goals.each do |goal| %>
    <% if goal.user == current_user %>
      <div class="card px-3">
        <img class="card-img-top" src="https://source.unsplash.com/random" alt="Card image cap">
        <div class="card-body">
          <h5 class="card-title">Title: <%= goal.title %></h5>
          <p class="card-text">Description: <%= goal.description %></p>
          <p class="card-text">Goal Amount: <%= goal.amount %></p>
        </div>
        <div class="card-footer">
          <small class="text-muted">TO DO: Days till completion</small>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
